apiVersion: v2
name: {{SERVICE_NAME}}
description: {{SERVICE_NAME}} worker chart — background queue consumer, thin wrapper over platform-base
type: application
version: 0.1.0
dependencies:
  - name: platform-base
    version: {{PLATFORM_BASE_VERSION}}
    repository: file://../../../library/platform-base